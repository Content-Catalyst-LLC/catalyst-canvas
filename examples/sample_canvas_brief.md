# Sample Catalyst Canvas Brief

This example is generated from `data/catalyst_canvas_sample_input.json` using the Python core generator.

Run:

```bash
python3 python/catalyst_canvas_core.py \
  --input data/catalyst_canvas_sample_input.json \
  --markdown outputs/sample_canvas_brief.md
```

Generated outputs are written to `outputs/`, which is ignored except for `.gitkeep`.
